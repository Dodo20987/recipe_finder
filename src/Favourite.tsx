import {View} from "react-native"
import React from "react";
const Favourite = () => {
    //<MaterialIcons name = "favourite" size = {24} color = "red" />
    return <View className = "flex flex-row">
    </View>
}

export default Favourite;
